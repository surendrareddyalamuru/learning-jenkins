pipeline {
    agent any
    environment {
        NEWRELIC_API_KEY = credentials('ansible01')
    }
    stages {
        stage('Foo') {
            steps {
              echo 'Hello world'
            }
        }
    }
}