pipeline {
    agent {
      node { label 'workstation' }
    }
    environment {
        NEWRELIC_API_KEY = credentials('ansible01')
    }
    stages {
        stage('Foo') {
            steps {
              echo 'Hello world'
            }
        }
        stages {
                stage('Bar') {
                    steps {
                      echo 'Hello world'
                    }
                }
    }
}